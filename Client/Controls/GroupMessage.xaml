<UserControl x:Class="Client.Controls.GroupMessage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Controls"
             mc:Ignorable="d">
    <Grid HorizontalAlignment="Left" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Background="Beige" FontSize="14" Name="UserName"></Label>
        <Border Grid.Row="1" Background="Beige" CornerRadius="0, 0, 0, 1000" ></Border>
        <Border Grid.Column="1" Grid.RowSpan="2" Background="Beige" CornerRadius="0 , 10, 10, 0" >
            <Label Background="Beige" FontSize="14" Name="Teext"></Label>
        </Border>
    </Grid>
</UserControl>
